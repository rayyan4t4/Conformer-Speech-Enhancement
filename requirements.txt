# Streamlit Cloud / local app requirements
# Pinned to versions that install cleanly on Streamlit Community Cloud (free tier).
# CPU-only torch build keeps the deploy small and within free memory limits.
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit==1.38.0
torch==2.3.1
numpy==1.26.4
soundfile==0.12.1
librosa==0.10.2.post1
matplotlib==3.9.0
scipy==1.13.1
